#include<stdio.h>
#include<conio.h>
void main()
{
    int temp, humidity;
    clrscr();
    printf("Enter temperature (°C) and humidity (%): ");
    scanf("%d%d",&temp,&humidity);
    if(temp>35 && humidity>70)
        printf("Heat and Humid Alert!");
    else
        printf("Environment OK");
    getch();
}
